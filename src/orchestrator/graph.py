from langgraph.graph import StateGraph

from src.orchestrator.state import GlobalState


def build_graph() -> StateGraph | None:
    # TODO: wire up nodes and edges for the full EAKIS pipeline
    return None
